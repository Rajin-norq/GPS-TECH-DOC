[build]
  command = "pip install -r requirements.txt && sphinx-build -b html docs/source docs/build/html"
  publish = "docs/build/html"

[build.environment]
  PYTHON_VERSION = "3.11"
  NETLIFY_USE_LEGACY_BUILD_IMAGE = "true"